from __future__ import annotations
import argparse
import random
from pathlib import Path
import pandas as pd


def build_prompt_for_chunk(row: pd.Series) -> str:
    return f"""Сгенерируй eval-вопросы для проверки end-to-end AI юридического помощника по этому юридическому чанку.

Это НЕ проверка отдельного retrieval и НЕ проверка ответа по уже данному контексту.
Нужно проверить всю систему целиком:
- понял ли помощник самостоятельный юридический вопрос пользователя;
- смог ли найти нужный документ и фрагмент;
- дал ли корректный правовой вывод;
- не выдумал ли факты вне источника;
- сослался ли на правильный документ;
- ответил ли достаточно полно и аккуратно.

КРИТИЧЕСКИ ВАЖНО:
Вопрос пользователя в поле question должен быть самодостаточным.
Пользователь НЕ видит DOC_ID, CHUNK_ID, TITLE и CHUNK_TEXT.
Поэтому question должен содержать достаточно поисковых якорей, чтобы по нему можно было найти нужный документ, норму или правовой фрагмент.

Каждый question должен содержать минимум 2-4 поисковых якоря из следующих:
- название или краткое описание акта, правил, регламента, порядка, положения;
- сфера регулирования;
- объект регулирования;
- орган или субъект обязанности;
- территория;
- организация;
- вид тарифа, субсидии, услуги, контроля, процедуры;
- ключевое действие, срок, условие или правовой эффект.

Если TITLE содержит только номер, технический идентификатор или OCR-мусор, используй в question понятное описание акта, процедуры, услуги или сферы регулирования из CHUNK_TEXT, а не только TITLE.

Запрещено в поле question использовать контекстные формулировки, понятные только из уже данного фрагмента:
- "этот фрагмент";
- "данный фрагмент";
- "приведенный фрагмент";
- "этот документ";
- "данный документ";
- "этот приказ";
- "данный приказ";
- "это постановление";
- "данное постановление";
- "эта услуга";
- "данная услуга";
- "эта организация";
- "данная организация";
- "указанные документы";
- "указанные субсидии";
- "указанные тарифы";
- "соответствующие документы";
- "соответствующие объекты";
- "генеральная схема" без уточнения сферы или акта;
- "тарифы" без указания объекта, территории, организации или вида тарифа;
- "субсидия" без указания вида субсидии;
- "государственная услуга" без указания конкретной услуги.

Также запрещены любые местоименные ссылки без явного объекта:
- "этот", "эта", "это", "эти";
- "данный", "данная", "данное", "данные";
- "указанный", "указанная", "указанное", "указанные";
- "соответствующий", "соответствующая", "соответствующее", "соответствующие";
- "он", "она", "они", "его", "их", "такой", "такая", "такие";
если без TITLE и CHUNK_TEXT непонятно, о чем речь.

Плохо:
- "Можно ли на основании данного фрагмента определить размер тарифа?"
- "Кто проверяет соответствие указанным критериям?"
- "В какой период действуют тарифы?"
- "Можно ли считать генеральную схему решением о строительстве?"
- "Какие документы должен представить заявитель по этой услуге?"
- "Кто утверждает перечень объектов по данному положению?"

Хорошо:
- "Можно ли определить конкретный размер тарифа на горячую воду для ООО «ТеплоВодоРесурс» на территории Мамаканского муниципального образования по приказу службы по тарифам Иркутской области?"
- "Кто проверяет соответствие муниципальных образований Иркутской области критериям отбора для субсидии на разработку схем теплоснабжения?"
- "В какой период действуют тарифы на горячую воду для ООО «ТеплоВодоРесурс» на территории Мамаканского муниципального образования?"
- "По Правилам разработки и утверждения документов перспективного развития электроэнергетики можно ли считать генеральную схему непосредственным решением о строительстве атомных электростанций, гидроэлектростанций или гидроаккумулирующих электростанций?"
- "Какими способами заявитель может подать документы для утверждения нормативов запасов топлива на источниках тепловой энергии в Иркутской области?"
- "Кто согласовывает перечень объектов, охраняемых ведомственной охраной оптовой генерирующей компании с генерирующими объектами гидроэлектростанций?"

Требования:
1. Верни только один JSON без пояснений:
   - либо JSON-массив объектов;
   - либо JSON-объект {{"resample": true, "reason": "..."}}.
2. JSON должен быть строго валидным:
   - используй только обычные двойные кавычки ";
   - не используй типографские кавычки “ ”;
   - не добавляй markdown, кодовые блоки, комментарии или пояснения.
3. Главный приоритет — качество самостоятельных юридических вопросов и эталонных ответов.
4. Лучше 1-3 сильных вопроса, чем много слабых.
5. Если чанк неудачный для генерации качественных юридических QA-примеров:
   - слишком технический;
   - оборванный;
   - OCR-мусор;
   - пустой по смыслу;
   - содержит только реквизиты;
   - содержит только таблицу без понятных заголовков периодов или условий;
   - не позволяет сформировать надежный правовой ответ;
   - позволяет сформировать только контекстный вопрос вида "по этому фрагменту";
   верни JSON-объект:
   {{"resample": true, "reason": "..."}}
6. Не выдумывай факты вне текста чанка и заголовка документа.
7. Эталонный ответ должен быть основан только на TITLE и CHUNK_TEXT.
8. Если по TITLE и CHUNK_TEXT нельзя дать полный ответ, можно создать вопрос типа negative_or_insufficient_basis, но сам question все равно должен быть самодостаточным и содержать поисковые якоря.
9. Вопросы должны быть похожи на реальные вопросы пользователя к юридическому AI-помощнику.
10. Не делай вопросы, которые требуют знания других актов, если эти акты прямо не упомянуты в чанке.
11. Ответ должен быть юридически аккуратным: сначала краткий вывод, затем основание из документа.
12. Не превращай expected_answer в огромный пересказ чанка. Он должен быть достаточным, но компактным.
13. Все строки внутри JSON должны быть корректно экранированы.
14. Не указывай конкретные размеры тарифов, суммы, значения показателей или перечни приложений, если они не приведены в TITLE и CHUNK_TEXT.
15. Не делай вопрос, который невозможно задать без предварительного показа пользователю данного чанка.
16. Не делай вопрос, если для ответа нужен полный документ, а в CHUNK_TEXT есть только обрывочный фрагмент без достаточного правового смысла.

Типы вопросов:
- direct_legal_fact: прямой вопрос на юридически значимый факт из нормы;
- procedure_or_condition: порядок, условия, сроки, основания, случаи применения;
- power_or_authority: полномочия, компетенция, ответственный орган, субъект обязанности;
- definition_or_scope: определение, сфера действия, круг лиц, объектов или отношений;
- list_extraction: перечень документов, действий, оснований, категорий, требований;
- application_to_situation: применение нормы к простой практической ситуации;
- negative_or_insufficient_basis: самодостаточный вопрос, где по найденному источнику нельзя сделать заявленный вывод или нужен осторожный ответ.

Не обязательно использовать все типы. Выбирай только те, которые естественно следуют из чанка.

Сложность:
- easy: ответ прямо указан в одном предложении или пункте;
- medium: нужно собрать 2-4 связанных положения из чанка;
- hard: нужно аккуратно структурировать порядок, условия, исключения или применить норму к ситуации.

Для каждого элемента JSON заполни поля:
- id: пустая строка;
- question;
- question_type;
- difficulty;
- expected_behavior: answer / insufficient_basis;
- expected_answer;
- gold_points;
- source_doc_id;
- source_chunk_id;
- source_title;
- category;
- relevant_doc_ids;
- relevant_chunk_ids;
- must_cite;
- must_not_claim;
- comment.

Правила заполнения:
- id: всегда пустая строка.
- question: самостоятельный естественный вопрос пользователя с поисковыми якорями.
- question_type: один из разрешенных типов.
- difficulty: easy, medium или hard.
- expected_behavior: answer, если по TITLE и CHUNK_TEXT можно дать ответ; insufficient_basis, если правильное поведение — ограничить вывод.
- expected_answer: эталонный ответ, который должен дать AI-юрист.
- gold_points: массив обязательных смысловых пунктов, без которых ответ неполный.
- source_doc_id: текущий DOC_ID.
- source_chunk_id: текущий CHUNK_ID.
- source_title: используй TITLE без искажения, если TITLE содержательный; если TITLE технический или содержит только номер, используй его как есть, но не выдумывай полное название.
- category: текущая CATEGORY.
- relevant_doc_ids: всегда включай текущий DOC_ID.
- relevant_chunk_ids: всегда включай текущий CHUNK_ID.
- must_cite: массив источников, пунктов или реквизитов, которые желательно упомянуть в ответе.
- must_not_claim: массив утверждений, которые нельзя делать, потому что они не следуют из чанка.
- comment: кратко объясни, что именно проверяет вопрос.

Перед финальным ответом молча проверь каждый question:
- его можно понять без TITLE и CHUNK_TEXT;
- в нем есть минимум 2 поисковых якоря;
- в нем нет запрещенных контекстных ссылок;
- по нему можно найти нужный акт, норму или правовой фрагмент через поиск;
- expected_answer не содержит фактов, которых нет в TITLE и CHUNK_TEXT.
Если question не проходит проверку — перепиши его или верни resample.

Формат ответа:
[
  {{
    "id": "",
    "question": "...",
    "question_type": "procedure_or_condition",
    "difficulty": "medium",
    "expected_behavior": "answer",
    "expected_answer": "...",
    "gold_points": [
      "...",
      "..."
    ],
    "source_doc_id": "{row["doc_id"]}",
    "source_chunk_id": "{row["chunk_id"]}",
    "source_title": "...",
    "category": "{row["category"]}",
    "relevant_doc_ids": ["{row["doc_id"]}"],
    "relevant_chunk_ids": ["{row["chunk_id"]}"],
    "must_cite": [
      "..."
    ],
    "must_not_claim": [
      "..."
    ],
    "comment": "..."
  }}
]

DOC_ID: {row["doc_id"]}
CHUNK_ID: {row["chunk_id"]}
CATEGORY: {row["category"]}
TITLE: {row["title"]}

CHUNK_TEXT:
{row["chunk_text"]}
"""


def parse_args() -> argparse.Namespace:
    parser = argparse.ArgumentParser(
        description="Pick one random legal chunk from chunks.parquet and generate LLM eval prompt."
    )
    parser.add_argument(
        "--chunks-path",
        type=Path,
        default=Path("data/processed/chunks.parquet"),
        help="Path to chunks.parquet",
    )
    parser.add_argument(
        "--seed",
        type=int,
        default=None,
        help="Random seed for reproducible sampling",
    )
    parser.add_argument(
        "--min-len",
        type=int,
        default=0,
        help="Optional minimum chunk length filter",
    )
    parser.add_argument(
        "--max-len",
        type=int,
        default=0,
        help="Optional maximum chunk length filter; 0 means no max filter",
    )
    parser.add_argument(
        "--category",
        type=str,
        default=None,
        help="Optional category filter, e.g. government / foiv / court",
    )
    parser.add_argument(
        "--save-prompt",
        type=Path,
        default=None,
        help="Optional path to save prompt txt",
    )
    parser.add_argument(
        "--show-chunk-chars",
        type=int,
        default=4000,
        help="How many chunk chars to print to console",
    )
    return parser.parse_args()


def main() -> None:
    args = parse_args()

    if not args.chunks_path.exists():
        raise FileNotFoundError(f"chunks file not found: {args.chunks_path}")

    df = pd.read_parquet(args.chunks_path)

    required_cols = {
        "doc_id",
        "title",
        "category",
        "chunk_id",
        "chunk_ix",
        "chunk_text",
        "chunk_len",
    }
    missing = required_cols - set(df.columns)
    if missing:
        raise ValueError(f"Missing required columns in parquet: {sorted(missing)}")

    if args.min_len > 0:
        df = df[df["chunk_len"] >= args.min_len].copy()

    if args.max_len > 0:
        df = df[df["chunk_len"] <= args.max_len].copy()

    if args.category:
        df = df[df["category"] == args.category].copy()

    df = df.reset_index(drop=True)

    if len(df) == 0:
        raise ValueError("No chunks left after filtering.")

    rng = random.Random(args.seed)
    idx = rng.randrange(len(df))
    row = df.iloc[idx]

    print("=" * 120)
    print("SELECTED CHUNK")
    print(f"CATEGORY: {row['category']}")
    print(f"DOC_ID:   {row['doc_id']}")
    print(f"CHUNK_ID: {row['chunk_id']}")
    print(f"TITLE:    {row['title']}")
    print(f"LEN:      {row['chunk_len']}")
    print("-" * 120)
    print(str(row["chunk_text"])[: args.show_chunk_chars])
    print("=" * 120)
    print()

    prompt = build_prompt_for_chunk(row)

    print("PROMPT")
    print("-" * 120)
    print(prompt)

    # if args.save_prompt is not None:
    #     args.save_prompt.parent.mkdir(parents=True, exist_ok=True)
    #     args.save_prompt.write_text(prompt, encoding="utf-8")
    #     print()
    #     print(f"Prompt saved to: {args.save_prompt.resolve()}")


if __name__ == "__main__":
    main()